<map
  class="full-map"
  latitude="{{latitude}}"
  longitude="{{longitude}}"
  scale="{{scale}}"
  markers="{{markers}}"
  bindmarkertap="onMarkerTap"
/>

<view class="empty-map" wx:if="{{!loading && markers.length === 0}}">
  暂无已标注位置的招领单
</view>
